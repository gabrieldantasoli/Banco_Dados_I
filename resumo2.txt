QUANTIFICADORES: ANY , ALL
    SELECT mat, salario
    FROM empregado
    WHERE salario >= all
    (SELECT salario FROM empregado);

Natural join : junção de tabelas por mesmo atributo;
Cross join : implementa o produto cartesiano;
Conditional Join : juncão por condição;
(left/right/full) outer join : juncao natural que preserva os resultados q nao casam;


