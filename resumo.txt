DML - Data Manipulation Lnaguage 


Select (From , Where , Distinct)
    - select X from Y where Z;

    + Operações de conjuntos (Union , Intersect , Except (Minus in Oracle) );
        Essas operações eliminam duplicatas, use ALL após elas para pegar todas as duplicatas.
        
        IN (está no sub-conjunto);
        EXISTS (retorna true se o argumento da subquery é nn vazio);

        IS NULL;
    Funções SQL (COUNT , SUM , AVG , MAX , MIN);

    GROUP BY (agrupa por algum(s) atributo);
    HAVING (usada em conjunto com GROUP BY, permite a inclusão de condições nos grupos);
    EX:{SELECT p.codproj, p.nome, COUNT(*)
        FROM Projeto p, Alocacao a
        WHERE p.codproj = a.codproj
        GROUP BY p.codproj, p.nome
        HAVING COUNT(*) > 2;}

    BETWEEN : algo between x and y;
    LIKE (% e _);
    UPPER() e LOWER();
    ORDER BY;